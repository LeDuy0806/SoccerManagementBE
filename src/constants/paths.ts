const API = '/api';

export const PATHS = {
    AUTH: `${API}/auth`,
    PRIZE: `${API}/prize`,
    STADIUM: `${API}/stadium`,
    REFEREE: `${API}/referee`,
    ROUND: `${API}/round`,
    TABLE: `${API}/table`,
};
